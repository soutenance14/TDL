<form method="post" action="{{ path('task_delete', {'id': task.id}) }}" onsubmit='return confirm("Etes vous certain de vouloir supprimer la tâche \" {{task.title}}\"  ?");'>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task.id) }}">
    <button class="btn btn-danger btn-sm pull-left">Supprimer</button>
</form>
